{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://tribus-erp.local/schemas/ingestion-payload-1.0.json",
  "title": "Tribus ERP — Ingestion payload",
  "description": "v1.0 · mode create. data fields use camelCase (ERP API). Max 200 objects. See docs/reference/ingestion-spec.md.",
  "x-ai-instructions": [
    "Use clear human references when reading user prompts, e.g. PRD-0007 (product), PRV-0002 (variant), stable SKUs for stock and orders.",
    "externalRef values (PRD-NNNN, PRV-NNNN, …) are generated by the ERP on create, per organization and per entity type in sequence (PREFIX-0001, PREFIX-0002…).",
    "For new objects in the same payload, prefer client_ref and *_ref / variantRef links; use real UUIDs in …Id fields when pointing at records that already exist in the database.",
    "Variable products (productKind: variable) require variantId or variantRef on inventory_movement and order line items when productRef targets that product in the same batch; see ingestion-spec.md."
  ],
  "type": "object",
  "additionalProperties": false,
  "required": ["version", "mode", "objects"],
  "properties": {
    "version": { "const": "1.0" },
    "mode": { "const": "create" },
    "objects": {
      "type": "array",
      "minItems": 1,
      "maxItems": 200,
      "items": { "$ref": "#/$defs/ingestionObject" }
    }
  },
  "$defs": {
    "ingestionObject": {
      "oneOf": [
        { "$ref": "#/$defs/stock_location_obj" },
        { "$ref": "#/$defs/category_obj" },
        { "$ref": "#/$defs/collection_obj" },
        { "$ref": "#/$defs/party_obj" },
        { "$ref": "#/$defs/customer_obj" },
        { "$ref": "#/$defs/supplier_obj" },
        { "$ref": "#/$defs/product_obj" },
        { "$ref": "#/$defs/product_variant_obj" },
        { "$ref": "#/$defs/product_composition_obj" },
        { "$ref": "#/$defs/inventory_movement_obj" },
        { "$ref": "#/$defs/order_obj" },
        { "$ref": "#/$defs/purchase_order_obj" }
      ]
    },
    "envelope": {
      "type": "object",
      "required": ["type", "data"],
      "properties": {
        "type": { "type": "string" },
        "client_ref": { "type": "string" },
        "data": { "type": "object" }
      }
    },
    "stock_location_obj": {
      "allOf": [
        { "$ref": "#/$defs/envelope" },
        {
          "properties": {
            "type": { "const": "stock_location" },
            "data": {
              "type": "object",
              "required": ["name", "type"],
              "properties": {
                "name": { "type": "string" },
                "type": {
                  "type": "string",
                  "enum": ["main", "event", "production", "damaged", "reserved", "third_party"]
                },
                "address": { "type": "string" }
              }
            }
          }
        }
      ]
    },
    "category_obj": {
      "allOf": [
        { "$ref": "#/$defs/envelope" },
        {
          "properties": {
            "type": { "const": "category" },
            "data": {
              "type": "object",
              "required": ["name", "slug"],
              "properties": {
                "name": { "type": "string" },
                "slug": { "type": "string" },
                "parentId": { "type": "string" },
                "parentCategoryRef": { "type": "string" },
                "description": { "type": "string" }
              }
            }
          }
        }
      ]
    },
    "collection_obj": {
      "allOf": [
        { "$ref": "#/$defs/envelope" },
        {
          "properties": {
            "type": { "const": "collection" },
            "data": { "type": "object", "required": ["name", "slug"] }
          }
        }
      ]
    },
    "party_obj": {
      "allOf": [
        { "$ref": "#/$defs/envelope" },
        {
          "properties": {
            "type": { "const": "party" },
            "data": { "type": "object", "description": "createPartySchema (camelCase)" }
          }
        }
      ]
    },
    "customer_obj": {
      "allOf": [
        { "$ref": "#/$defs/envelope" },
        {
          "properties": {
            "type": { "const": "customer" },
            "data": { "type": "object", "description": "createCustomerWithPartySchema" }
          }
        }
      ]
    },
    "supplier_obj": {
      "allOf": [
        { "$ref": "#/$defs/envelope" },
        {
          "properties": {
            "type": { "const": "supplier" },
            "data": { "type": "object", "description": "createSupplierWithPartySchema" }
          }
        }
      ]
    },
    "product_obj": {
      "allOf": [
        { "$ref": "#/$defs/envelope" },
        {
          "properties": {
            "type": { "const": "product" },
            "data": {
              "type": "object",
              "description": "createProduct + productKind (simple|variable) + optional main_image_url, gallery_image_urls, categoryRef, collectionRef"
            }
          }
        }
      ]
    },
    "product_variant_obj": {
      "allOf": [
        { "$ref": "#/$defs/envelope" },
        {
          "properties": {
            "type": { "const": "product_variant" },
            "data": {
              "type": "object",
              "required": ["productRef", "sku"],
              "description": "createVariant omit productId; parent must be productKind variable in same batch. Optional name, salePriceCents, attributes.",
              "properties": {
                "productRef": { "type": "string" },
                "sku": { "type": "string" },
                "attributes": { "type": "object", "additionalProperties": { "type": "string" } }
              }
            }
          }
        }
      ]
    },
    "product_composition_obj": {
      "allOf": [
        { "$ref": "#/$defs/envelope" },
        {
          "properties": {
            "type": { "const": "product_composition" },
            "data": {
              "type": "object",
              "required": ["parentProductRef", "quantity", "compositionType"],
              "properties": {
                "parentProductRef": { "type": "string" },
                "childProductRef": { "type": "string" },
                "childSku": { "type": "string" }
              }
            }
          }
        }
      ]
    },
    "inventory_movement_obj": {
      "allOf": [
        { "$ref": "#/$defs/envelope" },
        {
          "properties": {
            "type": { "const": "inventory_movement" },
            "data": {
              "type": "object",
              "required": ["type", "quantity"],
              "properties": {
                "productId": { "type": "string" },
                "productRef": { "type": "string" },
                "variantId": { "type": "string" },
                "variantRef": {
                  "type": "string",
                  "description": "client_ref of product_variant in same payload when productRef is variable in-batch"
                },
                "locationId": { "type": "string" },
                "locationRef": { "type": "string" }
              }
            }
          }
        }
      ]
    },
    "order_obj": {
      "allOf": [
        { "$ref": "#/$defs/envelope" },
        {
          "properties": {
            "type": { "const": "order" },
            "data": {
              "type": "object",
              "description": "createOrder + customerRef; items[].productRef; items[].variantRef / variantId when line targets variable product in-batch"
            }
          }
        }
      ]
    },
    "purchase_order_obj": {
      "allOf": [
        { "$ref": "#/$defs/envelope" },
        {
          "properties": {
            "type": { "const": "purchase_order" },
            "data": { "type": "object", "description": "createPurchaseOrder + supplierRef" }
          }
        }
      ]
    }
  }
}
